<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title><%= assigns[:page_title] || "Pink Ivory Foundry· Phoenix Framework" %></title>
    <link rel="stylesheet" href="<%= ~p"/assets/app.css" %>"/>
    <%= csrf_meta_tag() %>
</head>
<body>
<header>
    <section class="container">
        <nav role="navigation">
            <ul>
                <%= if @current_user do %>
                <li><%= @current_user.username %></li>
                <li><%= link "My Videos", to: ~p"/manage/videos" %></li>
                <li>
                    <%= link "Log out",
                    to: ~p"/sessions/#{@current_user}",
                    method: "delete" %>
                </li>
                <% else %>
                <li><%= link "Register", to: ~p"/users/new" %></li>
                <li><%= link "Log in", to: ~p"/sessions/new" %></li>
                <% end %>
            </ul>
        </nav>
        <a href="http://phoenixframework.org/" class="phx-logo">
            <img src="<%= ~p"/images/nobori.png" %>"
                 alt="Pink Ivory Foundry"/>
        </a>
    </section>
</header>
<main role="main" class="container">
    <p class="alert alert-info" role="alert"><%= Phoenix.Flash.get(@flash, :info) %></p>
    <p class="alert alert-danger" role="alert"><%= Phoenix.Flash.get(@flash, :error) %></p>
    <%= render @view_module, @view_template, assigns %>
</main>
<script>window.userToken = "<%= assigns[:user_token] %>"</script>
<script type="text/javascript"
        src="<%= ~p"/assets/app.js" %>"></script>
</body>
</html>
